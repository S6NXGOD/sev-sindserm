# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NUNCA comitar segredos (DATABASE_URL, SESSION_SECRET, etc.)
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local
.env.test
.env*.local
# o modelo SEM segredos PODE (e deve) ser versionado:
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma / bancos locais (SQLite, se usado em dev)
/prisma/*.db
/prisma/*.db-journal
*.sqlite
*.sqlite3
*.db

# logos estáticas: APENAS a logo padrão é versionada (as demais são por pleito,
# enviadas pelo admin para public/uploads/logos).
/public/logos/*
!/public/logos/logo_default.png

# logos enviadas pelo admin (mantém a pasta, ignora o conteúdo)
/public/uploads/logos/*
!/public/uploads/logos/.gitkeep
